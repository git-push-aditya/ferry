# ============================================================================
# PARAMS for aws/s3/sync-bucket-contents — NO CREDENTIALS.
#
# Both buckets must already exist — this integration creates neither.
# ============================================================================

SOURCE_S3_BUCKET_NAME=
DESTINATION_S3_BUCKET_NAME=
# Optional. Leave empty to sync every key.
KEY_PREFIX_FILTER=
