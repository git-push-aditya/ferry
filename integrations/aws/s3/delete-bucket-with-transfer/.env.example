# ============================================================================
# PARAMS for aws/s3/delete-bucket-with-transfer — NO CREDENTIALS.
#
# The destination bucket must already exist — run aws/s3/create-bucket first
# if it doesn't. This integration never creates it silently.
# ============================================================================

SOURCE_S3_BUCKET_NAME=
DESTINATION_S3_BUCKET_NAME=
