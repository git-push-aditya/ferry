# ============================================================================
# PARAMS for aws/iam/user/remove-user-from-group — NO CREDENTIALS.
#
# Safe to run even if the user or group is already gone — idempotent no-op.
# ============================================================================

IAM_USER_NAME=
IAM_GROUP_NAME=
