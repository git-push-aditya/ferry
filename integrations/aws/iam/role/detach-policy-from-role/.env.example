# ============================================================================
# PARAMS for aws/iam/role/detach-policy-from-role — NO CREDENTIALS.
#
# Safe to run even if the role or policy is already gone — idempotent no-op.
# ============================================================================

ROLE_NAME=
# Full ARN — AWS-managed (arn:aws:iam::aws:policy/...) or customer-managed
# (arn:aws:iam::<account-id>:policy/...).
POLICY_ARN=
