# ============================================================================
# PARAMS for aws/ec2/update-instance-type — NO CREDENTIALS.
#
# This integration operates on an instance that already exists — it does not
# launch one. Run aws/ec2/launch-instance first if you need it provisioned.
# ============================================================================

INSTANCE_ID=
# e.g. t3.large. The instance is stopped (if not already), resized, and
# restarted only if it was originally running.
TARGET_INSTANCE_TYPE=
