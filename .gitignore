node_modules/

# Credentials.
.env
.env.backend

# Params — one per integration folder. The .env.example beside each is committed.
integrations/*/*/.env

snowflake.log

# Reports (chmod 0600, may contain masked secret-derived values and resource identifiers).
output/
